import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['ebkmleylaghxmtfquvyf.supabase.co'], // Tambahkan hostname Supabase Anda di sini
  },
};

export default nextConfig;
